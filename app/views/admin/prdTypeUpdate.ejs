<!DOCTYPE html>
<html lang="zh-CN">
    <head>
        <% include inc/meta.ejs %>

        <title><%= title %></title>

        <% include inc/headerlink.ejs %>
    </head>
    <body>
        <% include inc/nav.ejs %>


        <div class="container">
          
          <% if(msg && msg.length>0) {%>
          <div class="alert alert-danger" role="alert"><%= msg %></div>
          <%}%>

          <form id="prdTypeAdd" class="form-horizontal" method="POST">
              <div class="form-group">
                <label for="prdTypename" class="col-sm-2 control-label">分类名称</label>
                <div class="col-sm-8">
                  <input name="name" type="text" class="form-control" placeholder="分类名称" value="<%=prdType.name%>">
                </div>
              </div>
               <div class="form-group">
                <div class="col-sm-offset-2 col-sm-8">
                    <input class="btn btn-default" type="submit" value="保存"/>
                    <a class="btn btn-danger" href="/admin/prdType">返回</a>
                </div>
              </div>
            </form>
        </div>
</body>
</html>

